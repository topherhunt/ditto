# Copy to devops/deploy.env (gitignored) and edit.
DEPLOY_HOST=racknerd1
DEPLOY_USER=root
SSH_PORT=22
SERVICE_NAME=ditto
SERVICE_USER=ditto
DOMAIN=ditto.topherhunt.com
# Must be free on the shared VPS and never reused. Check: ssh racknerd1 'cat /srv/registry/*.app'
APP_PORT=3005
REMOTE_DIR=/srv/ditto
